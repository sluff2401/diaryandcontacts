{% extends "mysite/base.html" %}

{% block content %}

    <h2>
    <a class="btn btn-default" href="{% url 'contactlist' %}" >
    <span>View users</span></a>
    </h2>

  <div class="person">
    <a class="btn btn-default" href="{% url 'contact_process' pk=contact.pk function='update' %}">
    <span>Update this contact</span></a>
    {% if event_status_now = 'live' %}
      <a class="btn btn-default" href="{% url 'contact_process' pk=contact.pk function='deleteperm' %}">
      <span>Delete this contact</span></a>
    {% endif %}

    <h2 class="date">
    <span class="item">{{ contact.full_name  }}</span>
    </h2>

    {% if circles %}
      <h2 class="date">Circles</h2><h2 class="item">{{ circles }}</h2>
    {% endif %}
  </div>
{% endblock %}
{% comment %}
    <a class="btn btn-default" href="{% url 'homepage' %}">View Events</a>

      <a class="btn btn-default" href="{% url 'contactupdate' pk=person.pk %}">
      <span>Update this member</span></a>
      <a class="btn btn-default" href="{% url 'contactdelete' pk=person.pk confirmed='no' %}">
      <span>Remove this member</span></a>
{% endcomment %}
